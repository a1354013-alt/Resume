# Use a hoisted layout so Node resolves peer deps (react/react-dom) to a single module path.
# This prevents "Invalid hook call" errors in Node-based test runs under pnpm's isolated linker.
node-linker=hoisted
